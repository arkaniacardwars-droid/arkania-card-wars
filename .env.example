# Arkania Card Wars — variáveis de ambiente (copie para .env e NÃO commite o .env)

# --- cliente (expostas ao browser; prefixo PUBLIC_ no SvelteKit) ---
PUBLIC_SUPABASE_URL="https://xntxtdddkomcgzgvygss.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="sb_publishable_6j7XlXTIwv9Vheo5r8ewpg__0TIDMJP"

# --- servidor (NUNCA expor ao browser; só em Edge Functions / rotas +server) ---
# Painel Supabase → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY="cole-a-service-role-secret-aqui"

# Connection string direta — senha URL-encoded (@ vira %40).
# Troque a senha no painel depois do setup (ela foi compartilhada em texto puro).
DATABASE_URL="postgresql://postgres:arkania%402026@db.xntxtdddkomcgzgvygss.supabase.co:5432/postgres"
